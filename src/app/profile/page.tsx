import { SimpleShell } from "@/components/omnipath/support/SimpleShell";

export default function ProfilePage() {
  return (
    <SimpleShell
      eyebrow="Profile"
      title="Profile stays staged for the later account phase"
      summary="This route stays minimal until user-linked data and ownership rules are authorized."
    />
  );
}
