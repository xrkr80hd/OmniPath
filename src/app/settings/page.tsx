import { SimpleShell } from "@/components/omnipath/support/SimpleShell";

export default function SettingsPage() {
  return (
    <SimpleShell
      eyebrow="Settings"
      title="Settings stays simple while the core player flow locks in"
      summary="Audio, display, and account-level controls can stage here without interrupting character creation."
    />
  );
}
