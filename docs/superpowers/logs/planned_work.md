# planned_work

## LICL

`LICL` means:

1. `Log planned_work`
2. `Implement planned_work`
3. `Check planned_work log so no task is missed`
4. `Log to work_completed`

This log is the forward-looking execution ledger for OmniPath. It exists to keep the repo on one product direction and prevent missed tasks or duplicate builds.

## Active Plan

- Plan file: `Inline Execution`
- Approved design spec: `docs/superpowers/specs/2026-04-18-omnipath-command-center-design.md`
- Execution mode: `Inline Execution`
- Guardrail: Character creation must route through a realm-first gateway, Session Zero must stay anchored to the chosen realm, and the landing screen must keep one title image per visit while rotating to a different image only when the traveler returns.

## Task Queue

- [x] Task 1: Route character creation through a dedicated realm gateway and add realm-anchored Session Zero entry points
- [x] Task 2: Replace the stale companion shortcuts with saved-character loading paths and keep the preview handoff aligned with real saved characters
- [x] Task 3: Make the title screen hold one background image per visit and advance only on the next visit back to the landing page
- [x] Task 4: Verify the gateway flow, title-screen behavior, check the LICL queue for misses, and log completion

## Current Focus

- Current task: `Completed`
- Current phase: `Realm gateway flow, realm-anchored Session Zero routing, saved-character landing paths, and visit-based title-screen artwork are complete and ready for push`
